# 整站共用密碼登入所需的環境變數（此檔可 commit，作為部署對照範本）
# 實際數值請放到 .env.local（本機）或 Vercel 專案設定（正式）。
#
# 產生足夠隨機的 AUTH_SECRET，可用：
#   openssl rand -base64 32

# 登入帳號
APP_USERNAME=
# 登入密碼
APP_PASSWORD=
# 簽 session cookie 的密鑰（至少 32 字元的隨機字串）
AUTH_SECRET=
