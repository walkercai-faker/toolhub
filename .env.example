# 瀏覽器直連 Supabase 所需的環境變數（此檔可 commit，作為部署對照範本）
# 實際數值請放到 .env.local（本機）或 GitHub repo 的 Actions Variables（正式站）。
#
# 兩個值都在 Supabase 專案的 Settings → API 取得。
# NEXT_PUBLIC_ 開頭的變數會在 build 時被內嵌進前端 bundle：
# anon key 本來就設計成可公開，資料安全一律靠 Supabase RLS policy 把關。

# Supabase 專案網址，例：https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=
# Supabase anon / public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=
